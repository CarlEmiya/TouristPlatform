2.关于评论的功能	----     被评论者可能是旅游活动、旅行日记，所以给两者设定不同的id，（其实好像无所谓，毕竟在不同表里写）
3.对于彻底删除用户，需要考虑以下几点：不留外键，但是删用户的时候要删除通知信息，别的不删，notification
4.关于用户的评论回复问题

5.关于用户文件上传问题，用户编辑页面编辑完，点击提交之后，将整个页面用wangEditor编辑器编辑完,将整个页面存储在对应表的content字段中

6.2024/12/4,评论功能基本实现，页面的分页器、评分都做了，但是编辑、发布、举报  这几个涉及wangEditor的文件上传，所以没实现，做了个壳子，

7.关于修改等级之后，通知用户，UserController中，updateUserLevel方法，留了注释，但是没有实现，需要自己实现。
关联知识：https://www.wangeditor.com/v5/content.html#%E8%8E%B7%E5%8F%96%E5%86%85%E5%AE%B9
获取 HTML 和 Text
使用 editor.getHtml() 获取 HTML 内容，可参考 demo。使用 editor.getText() 获取纯文本内容。

推荐使用 HTML 格式存储数据。

#获取 JSON
使用 editor.children 获取 JSON 内容。

JSON 格式可以转换为 HTML 和 Text 格式，支持浏览器和 nodejs 。 如果是在 nodejs 中，需要安装 yarn add jsdom global-jsdom ，并且引入 require('global-jsdom/register')。

const editor = createEditor({ content }) // `content` 即为 JSON 内容
const html = editor.getHtml()
const text = editor.getText()




7.关于修改等级之后，通知用户
UserController中，updateUserLevel方法，留了注释，但是没有实现，需要自己实现。



需要问的：
14.删除标签，能把能删除tagsContainer内的标签，但是tags中没有删除应该被删除的标签


待做：
16.发布活动等  编辑活动的部分改好了，直接抄编辑

18.用户头像自行上传

19.
等级设定问题
将所有alert都换成模态框提示
过滤器：不登录只能去看首页，别的都不行
发布活动的用户在活动进行过程中可修改活动状态，随着当前时间变化，自动修改活动状态
数据库上多找几个旅游的景点图片填充进去
赞数 和 活动状态 自动更新 ✅

20.detail页面的取消活动的模态框关不上了

21.需要衔接的地方：
发布活动的用户在活动启动后可发送私信/消息给参与用户，以便发送通知。
组团满额后，活动状态自动改为“已成团”系统发送邮件或消息给发布活动的用户，提示已成团。
还有被修改等级，被封禁，恢复的时候都应该通知用户
管理员禁用的用户可登录，但是只看到提示界面，提示账户不可用，并可以发送消息给管理员请求解冻账户,留的方法：appeal，在ban.html中21行














40.通过演示规避的bug：
活动详情页面评论区不要点分页导航
用户个人详情页面，点头像可以上传文件，但是没有跟控制器对接好uploadAvatar，方法写的也不好，没有做向数据库里改用户头像的操作。   这还涉及一个读取问题，得绝对转相对，然后再读取

